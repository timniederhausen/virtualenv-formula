virtualenv
==========

Formula to install the virtualenv package.

Available states
================

.. contents::
    :local:

``virtualenv``
--------------

Install and configure the virtualenv package.
